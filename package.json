{
  "name": "mcp-bun-streamable-starter",
  "version": "1.0.0",
  "description": "MCP Streamable HTTP transport starter template using Bun",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "bun --bun tsc --noEmit"
  },
  "keywords": ["mcp", "model-context-protocol", "streamable", "http", "bun"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "@the-ihor/mcp-sdk-typescript": "^0.0.1",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}